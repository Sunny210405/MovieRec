import { defineConfig } from 'vite'

export default defineConfig({
  // For GitHub Pages project repository, set base to '/repository-name/'
  // For GitHub Pages user/org site, set base to '/'
  // Update 'MovieRec' to your actual repository name
  base: '/MovieRec/',
})
